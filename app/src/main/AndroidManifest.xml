<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.a.appedenfire">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/ELeden"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InicioActivity" />
        <activity android:name=".HabitacionActivity" />
        <activity android:name=".RestauranteActivity" />
        <activity android:name=".EventosActivity" />
        <activity android:name=".OfertaActivity" />
        <activity android:name=".ReservaOfertaActivity" />
        <activity android:name=".InstalacionServiciosActivity" />
        <activity android:name=".LugarActivity" />
        <activity android:name=".ReservaHabitacionActivity" />
        <activity android:name=".Admin" />

    </application>

</manifest>